import React, { Component } from "react";

class Survey extends Component {
  componentDidMount() {
    // Fetches the survey config data.
  }

  render() {
    return <div>Survey</div>;
  }
}

export default Survey;
